import type { SiteConfig } from "@nuucognition/docs-theme";

export const siteConfig: SiteConfig = {
  name: "Flint Server Docs",
  description: "Documentation for Flint Server - the local HTTP backend for Flint workspaces",
  basePath: "/docs",
};
